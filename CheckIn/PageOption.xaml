<Page
    x:Class="CheckIn.PageOption"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CheckIn"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Line ></Line>
            <TextBlock Name="TbTime" HorizontalAlignment="Center" Text="签到时间:2,4,14,20"  VerticalAlignment="Top" FontSize="48" Grid.Row="1" Grid.ColumnSpan="3" />
        <ComboBox Name="CbDayOfWeek" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="140" FontSize="36" SelectionChanged="CbDayOfWeek_SelectionChanged" Grid.Row="1" Grid.Column="1" Height="60"/>
        <ComboBox Name="CbCheckKind" HorizontalAlignment="Right"  VerticalAlignment="Bottom"  Width="140" FontSize="36" SelectionChanged="CbCheckKind_SelectionChanged" Grid.Row="1" Grid.Column="1"  Height="60" />
    </Grid>
</Page>
