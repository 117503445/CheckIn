<Page
    x:Class="CheckIn.PageAdmin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CheckIn"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid x:Name="grid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <GridView Name="Gv"  Grid.Row="1" Grid.Column="0"/>
        <ComboBox x:Name="CboFile" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="2"/>
        <TextBlock x:Name="TxtWrong" Text="读取错误" Grid.Row="1" Grid.Column="0" FontSize="144" HorizontalAlignment="Center" VerticalAlignment="Center" d:IsHidden="True"/>
        <Button Name="BtnChangeSeat"  Content="换座位" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="20" Click="BtnChangeSeat_Click"/>
    </Grid>

</Page>
